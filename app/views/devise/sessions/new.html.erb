<h1 class="text-2xl mb-2">Log in</h1>
<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" },
                class: 'mb-4' %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Enter", class: "sign-button" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>


<%# <div class="form-inputs">
  <div class="input email optional user_email">
    <label class="email optional" for="user_email">Email</label>
    <input autocomplete="email" class="string email optional" autofocus="autofocus" type="email" value="" name="user[email]" id="user_email">
  </div>
</div>
<div class="input password optional user_password">
  <label class="password optional" for="user_password">Password</label>
  <input autocomplete="current-password" class="password optional" type="password" name="user[password]" id="user_password">
</div>

<div class="form-actions">
  <input type="submit" name="commit" value="Enter" class="sign-button" data-disable-with="Enter">
</div> %>
